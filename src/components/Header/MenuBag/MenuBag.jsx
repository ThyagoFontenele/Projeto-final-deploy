import React from 'react'
import './MenuBag.css'

function MenuBag(props) {
    return (props.trigger) ? (
        <div>
          <nav id="bag-container">
                <div>
                  <h1>dadsdad</h1>
                </div>
            </nav>   
        </div>
    ) : "";
}

export default MenuBag
